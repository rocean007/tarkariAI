# ─── Tarkari AI Model Backend ───────────────────────────────────────────────
# OpenAI-compatible endpoint (works with OpenAI, HuggingFace, vLLM, Ollama) for Tarkari AI
TARKARI_AI_BASE_URL=https://api.openai.com/v1
TARKARI_AI_API_KEY=sk-your-tarkari-ai-api-key-here
TARKARI_AI_MODEL=gpt-4o-mini

# For HuggingFace Inference Endpoints:
# TARKARI_AI_BASE_URL=https://your-endpoint.endpoints.huggingface.cloud/v1
# TARKARI_AI_API_KEY=hf_your_token_here
# TARKARI_AI_MODEL=tgi

# For Ollama (local):
# TARKARI_AI_BASE_URL=http://localhost:11434/v1
# TARKARI_AI_API_KEY=ollama
# TARKARI_AI_MODEL=llama3.2:3b

# ─── API Authentication ───────────────────────────────────────────────────────
# Salt used for hashing API keys (generate with: openssl rand -hex 32)
API_SECRET_SALT=change-me-to-a-random-secret-string-at-least-32-chars

# Pre-issued API keys (comma-separated, no spaces)
# These are the keys your external apps will use to call /api/v1/completions
VALID_API_KEYS=myai-key-prod-abc123,myai-key-dev-xyz789

# ─── CORS / Authorized Origins ───────────────────────────────────────────────
# Comma-separated list of domains allowed to call the REST API
# Use * to allow all origins (not recommended for production)
ALLOWED_ORIGINS=https://yourapp.com,https://another-site.com

# ─── App Config ──────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=Tarkari AI Assistant
NEXT_PUBLIC_APP_DESCRIPTION=Your personal Tarkari AI assistant, powered by fine-tuned open-source models.

# ─── Optional: System Prompt ─────────────────────────────────────────────────
SYSTEM_PROMPT=You are a helpful, harmless, and honest Tarkari AI assistant. You are knowledgeable, concise, and always aim to provide accurate and useful responses.
